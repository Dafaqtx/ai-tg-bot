# Telegram Bot с использованием TypeScript, pnpm и Gemini API

Этот проект представляет собой Telegram бота, разработанного с использованием TypeScript и pnpm. Бот обрабатывает команды `/start` и `/help`, а также использует Gemini API для генерации ответов на сообщения пользователей.

## Функциональность

### Основные возможности

- Обработка команд `/start`, `/help` - базовая информация и справка
- **Персональные стили ответов** - `/style`, `/styles`, `/setstyle` для настройки стиля общения
- **Память диалога** - `/context`, `/clearcontext` для управления контекстом разговора
- Генерация ответов на текстовые сообщения с помощью Gemini API с учетом предпочтений пользователя и контекста
- **Автоматическое разделение длинных сообщений** - умное разбиение сообщений длиннее 4096 символов на части

### 🎨 Персонализация стилей

Бот поддерживает 10 различных стилей ответов:

**Основные стили:**

- **⚡ Краткий** (`concise`) - короткие и лаконичные ответы с эмоджи
- **😊 Дружелюбный** (`friendly`) - теплое и понимающее общение как с другом
- **📚 Подробный** (`detailed`) - развернутые информативные ответы с практическими советами
- **🧠 Экспертный** (`expert`) - системный анализ и структурированные ответы
- **🩺 Медицинский** (`medical`) - специализированные ответы по вопросам здоровья

**Специальные стили:**

- **🎓 Образовательный** (`educational`) - объяснения как для студентов с примерами
- **💪 Мотивирующий** (`motivational`) - вдохновляющие и поддерживающие ответы
- **💻 Программистский** (`developer`) - технические ответы с примерами кода
- **😄 Юмористический** (`humorous`) - развлекательные ответы с шутками
- **🧘 Спокойный** (`calm`) - расслабляющие ответы для снятия стресса

#### Команды персонализации

- `/style` - показать текущий стиль ответов
- `/styles` - показать все доступные стили с **интерактивными кнопками**
- `/setstyle [стиль]` - изменить стиль ответов (например: `/setstyle friendly`)

#### Удобный выбор стилей

- **Интерактивные кнопки** - просто нажмите на нужный стиль в команде `/styles`
- **Текстовые команды** - используйте `/setstyle` с названием стиля в скобках
- **Автосохранение** - ваши настройки запоминаются навсегда

### 🧠 Память диалога

Бот запоминает контекст разговора для более естественного общения:

**Основные возможности:**

- **Умная память** - запоминает до 20 последних сообщений или ~8000 токенов
- **Связный диалог** - может отвечать на вопросы, ссылаясь на предыдущие сообщения
- **Автоматическая очистка** - старые сообщения удаляются при превышении лимитов
- **Персональный контекст** - у каждого пользователя своя независимая история

#### Команды управления памятью

- `/context` - показать статистику и настройки контекста диалога
- `/clearcontext` - очистить всю историю сообщений и начать заново

#### Как это работает

1. **Запоминание** - каждое ваше сообщение и ответ бота сохраняются в памяти
2. **Контекст в запросах** - при генерации ответа бот учитывает предыдущие сообщения
3. **Умные ограничения** - память автоматически управляется для оптимальной работы
4. **Безопасность** - контекст хранится локально и не передается третьим лицам

#### Примеры использования

```
Пользователь: Расскажи про Python
Бот: Python - это язык программирования...

Пользователь: А какие у него преимущества?
Бот: У Python, о котором мы говорили, есть несколько ключевых преимуществ...
(бот помнит, что речь идет о Python)

Пользователь: Покажи пример кода
Бот: Вот пример кода на Python для демонстрации его простоты...
(бот помнит контекст разговора о Python)
```

### Автоматическое разделение сообщений 📝

- **Умное разбиение** - автоматическое разделение длинных ответов (>4096 символов) на части
- **Приоритетное разделение** - по абзацам → предложениям → словам (избегает разрезания слов)
- **Сохранение форматирования** - Markdown разметка остается корректной во всех частях
- **Нумерация частей** - удобные индикаторы "Продолжение (2/3)" для отслеживания
- **Оптимизированный UX** - небольшие задержки между частями для комфортного чтения

### Обработка аудио 🎵

- **Голосовые сообщения** - транскрипция и интеллектуальные ответы
- **Аудиофайлы** - анализ содержания (речь, музыка, подкасты)
- **Адаптивные промты** - разные стратегии для коротких и длинных сообщений
- **Поддержка форматов**: MP3, WAV, M4A, AAC, OGG, FLAC

### Технические особенности

- Обработка ошибок с детальной диагностикой
- Корректное завершение работы при получении сигналов остановки
- **Система логирования** - Winston с ротацией файлов и мониторингом
- Автоматическая очистка временных файлов
- Логирование активности пользователей и API вызовов

## Требования

- Node.js (версия 14 или выше)
- pnpm (версия 6 или выше)
- Токен Telegram бота (получается через @BotFather)
- API-ключ Gemini (получается на [Google AI Studio](https://ai.google.dev/))
- Прокси-сервер (опционально, для обхода географических ограничений)

## Установка

1. Клонируйте репозиторий:

```bash
git clone https://github.com/yourusername/ai-tg-bot.git
cd ai-tg-bot
```

2. Установите зависимости с помощью pnpm:

```bash
pnpm install
```

3. Создайте файл `.env` и добавьте токен вашего бота:

```env
BOT_TOKEN=your_bot_token_here
GEMINI_API_KEY=your_gemini_api_key_here
```

## Запуск

### Для разработки (с автоматической перезагрузкой при изменении файлов):

```bash
pnpm dev
```

### Для продакшена:

```bash
pnpm build
pnpm start
```

## Структура проекта

```
.
├── .env                # Файл с переменными окружения (не включен в репозиторий)
├── .eslintrc.js       # Конфигурация ESLint
├── .gitignore         # Список игнорируемых файлов для Git
├── README.md          # Документация проекта
├── package.json       # Файл с зависимостями и скриптами
├── pnpm-workspace.yaml # Конфигурация рабочего пространства pnpm
├── src/               # Исходный код
│   ├── index.ts       # Основной файл приложения (точка входа)
│   ├── types/         # Типы и интерфейсы TypeScript
│   │   └── index.ts   # Определения типов для всего приложения
│   ├── config/        # Конфигурация приложения
│   │   └── index.ts   # Настройки окружения и промты
│   ├── services/      # Бизнес-логика и сервисы
│   │   ├── index.ts   # Экспорт всех сервисов
│   │   ├── geminiService.ts    # Сервис для работы с Gemini API
│   │   ├── audioService.ts     # Обработка аудиосообщений
│   │   ├── imageService.ts     # Обработка изображений
│   │   └── loggerService.ts    # Система логирования
│   ├── handlers/      # Обработчики команд и сообщений
│   │   ├── index.ts   # Экспорт всех обработчиков
│   │   ├── commandHandlers.ts  # Обработчики команд (/start, /help)
│   │   └── messageHandlers.ts  # Обработчики сообщений (текст, аудио, фото)
│   └── utils/         # Утилиты и вспомогательные функции
│       ├── index.ts   # Экспорт всех утилит
│       ├── fileUtils.ts        # Работа с файлами и MIME типами
│       └── telegramUtils.ts    # Утилиты для Telegram (разделение сообщений, Markdown)
├── docs/              # Документация
│   └── LOGGING.md     # Документация по логированию
├── logs/              # Файлы логов (создается автоматически)
│   ├── combined-YYYY-MM-DD.log    # Все логи
│   ├── error-YYYY-MM-DD.log       # Только ошибки
│   ├── exceptions-YYYY-MM-DD.log  # Исключения
│   └── rejections-YYYY-MM-DD.log  # Отклоненные промисы
└── tsconfig.json      # Конфигурация TypeScript
```

## Доступные скрипты

- `pnpm run build` - Собрать проект
- `pnpm run start` - Запустить бота
- `pnpm run dev` - Разработка с горячей перезагрузкой
- `pnpm run dev:watch` - Разработка с отслеживанием всех файлов в src/
- `pnpm run clean` - Очистить папку dist
- `pnpm run build:clean` - Очистить и собрать заново
- `pnpm run lint` - Проверить код линтером
- `pnpm run lint:fix` - Исправить ошибки линтера
- `pnpm run type-check` - Проверить типы TypeScript без компиляции
- `pnpm run test` - Запустить тесты

## Как получить токен Telegram бота

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания нового бота
4. После создания бота вы получите токен, который нужно добавить в файл `.env`

## Как получить API-ключ Gemini

1. Перейдите на [Google AI Studio](https://ai.google.dev/)
2. Создайте аккаунт или войдите в существующий
3. Перейдите в раздел API Keys
4. Создайте новый API-ключ
5. Скопируйте полученный ключ и добавьте его в файл `.env`

## Тестирование аудио функций

### Голосовые сообщения

1. Запустите бота: `pnpm dev`
2. Отправьте **короткое голосовое сообщение** (до 60 сек) - получите естественный ответ
3. Отправьте **длинное голосовое сообщение** (более 60 сек) - получите структурированный анализ

### Аудиофайлы

1. Отправьте **музыкальный файл** - получите анализ стиля и настроения
2. Отправьте **подкаст или лекцию** - получите транскрипцию и краткое содержание
3. Поддерживаемые форматы: MP3, WAV, M4A, AAC, OGG, FLAC

### Ожидаемые результаты

- **Короткие голосовые**: Естественный диалог с эмоджи
- **Длинные голосовые**: Формат с транскрипцией, ключевыми моментами и ответами
- **Аудиофайлы**: Определение типа контента и детальный анализ

## Тестирование памяти диалога

### Как проверить функциональность контекста

1. Запустите бота: `pnpm dev`
2. Начните диалог с ботом:

   ```
   Пользователь: Привет! Меня зовут Алексей
   Бот: Привет, Алексей! Приятно познакомиться...

   Пользователь: Как меня зовут?
   Бот: Тебя зовут Алексей, как ты говорил ранее.
   ```

3. Проверьте команды управления контекстом:
   - `/context` - посмотреть статистику памяти
   - `/clearcontext` - очистить историю и начать заново

### Ожидаемые результаты

**Связный диалог:**

- Бот помнит имена, темы разговора, предыдущие вопросы
- Может ссылаться на ранее обсуждаемые темы
- Поддерживает естественный ход беседы

**Команда `/context`:**

```
🧠 Настройки контекста диалога

✅ Статус: Включен
📊 Сообщений в памяти: 8
🔢 Примерно токенов: 156

⚙️ Настройки:
• Максимум сообщений: 20
• Максимум токенов: 8000
• 🔄 Автоочистка: Включена

📅 Самое старое сообщение: 05.01.2025, 15:30:45

Используйте /clearcontext чтобы очистить историю диалога.
```

**Команда `/clearcontext`:**

```
🧹 Контекст очищен!

Удалено сообщений: 8

Теперь я не помню предыдущие сообщения. Можете начать новый диалог! 🚀
```

### Особенности работы памяти

- **Автоматическое управление** - старые сообщения удаляются при превышении лимитов
- **Персональная память** - у каждого пользователя своя независимая история
- **Умные ограничения** - до 20 сообщений или ~8000 токенов
- **Безопасность** - контекст хранится локально в файле `data/user-contexts.json`

## Тестирование разделения длинных сообщений

### Как проверить функциональность

1. Запустите бота: `pnpm dev`
2. Отправьте длинный текстовый запрос (например, "Расскажи подробно об истории искусственного интеллекта")
3. Если ответ превысит 4096 символов, бот автоматически разделит его на части

### Ожидаемый результат

```
[Часть 1]
Подробный ответ об искусственном интеллекте...

Продолжение следует... (1/3)

[Часть 2]
Продолжение (2/3)

Дальнейшая информация...

Продолжение следует...

[Часть 3]
Продолжение (3/3)

Заключительная часть ответа...
```

### Особенности разделения

- **Умное разбиение** - текст разделяется по абзацам, предложениям или словам
- **Сохранение форматирования** - Markdown остается корректным
- **Удобная навигация** - нумерация частей помогает отслеживать прогресс
- **Оптимальные задержки** - 500мс между частями для комфортного чтения

## Модификация и расширение

Проект имеет модульную архитектуру, что делает его легким для понимания и модификации:

### Добавление новых команд

1. Добавьте обработчик в `src/handlers/commandHandlers.ts`
2. Зарегистрируйте команду в `src/index.ts`

### Добавление новых типов сообщений

1. Определите новые типы в `src/types/index.ts`
2. Создайте сервис в `src/services/`
3. Добавьте обработчик в `src/handlers/messageHandlers.ts`

### Изменение промтов

Все промты находятся в `src/config/index.ts` и легко настраиваются

### Добавление утилит

Вспомогательные функции добавляются в `src/utils/`

### Преимущества модульной архитектуры

- **Разделение ответственности** - каждый модуль отвечает за свою задачу
- **Легкость тестирования** - модули можно тестировать изолированно
- **Переиспользование кода** - сервисы можно использовать в разных частях приложения
- **Простота расширения** - новая функциональность добавляется без изменения существующего кода

## Логирование

Проект использует комплексную систему логирования на базе Winston:

### Особенности

- **Ротация файлов** - автоматическое создание новых файлов каждый день
- **Разные уровни** - error, warn, info, debug
- **Структурированные логи** - JSON формат для удобного анализа
- **Мониторинг активности** - отслеживание действий пользователей
- **API мониторинг** - логирование времени выполнения запросов

### Файлы логов

```
logs/
├── combined-2025-07-05.log      # Все логи
├── error-2025-07-05.log         # Только ошибки
├── exceptions-2025-07-05.log    # Необработанные исключения
└── rejections-2025-07-05.log    # Отклоненные промисы
```

### Использование

```typescript
import { logger } from "./services";

// Базовые методы
logger.info("Информационное сообщение");
logger.error("Ошибка", error);
logger.warn("Предупреждение");
logger.debug("Отладочная информация");

// Специальные методы
logger.logUserActivity(userId, username, "action", { details });
logger.logSystemEvent("event_name", { data });
logger.logApiCall("service", "method", duration, success, error);
```

Подробная документация: [docs/LOGGING.md](docs/LOGGING.md)

## Качество кода

### Линтинг

Проект настроен с использованием ESLint для поддержания качества кода:

```bash
# Проверка кода
pnpm lint

# Автоматическое исправление проблем
pnpm lint:fix

# Проверка типов TypeScript
pnpm type-check
```

### Стандарты коммитов

Проект использует [Conventional Commits](https://www.conventionalcommits.org/) с автоматической проверкой:

```bash
# Проверить последний коммит
pnpm run commitlint:check

# Формат коммита
<type>[scope]: <description>

# Примеры
feat(telegram): add message splitting for long responses
fix(services): resolve gemini api timeout errors
docs: update commit guidelines
test(utils): add message splitting tests
```

**Типы коммитов:**

- `feat` - новая функциональность
- `fix` - исправление багов
- `docs` - документация
- `test` - тесты
- `refactor` - рефакторинг
- `style` - форматирование
- `perf` - оптимизация
- `build` - сборка/зависимости
- `ci` - CI/CD
- `chore` - обслуживание

**Области (scope):**

- `core`, `handlers`, `services`, `utils`, `config`
- `telegram`, `gemini`, `audio`, `image`, `logging`
- `tests`, `docs`, `deps`, `ci`

Подробнее: [docs/COMMIT_GUIDELINES.md](docs/COMMIT_GUIDELINES.md)

## Лицензия

ISC
